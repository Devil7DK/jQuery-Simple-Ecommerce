{
    "configurations": [
        {
            "name": "Launch Program",
            "program": "${workspaceFolder}/server/app.js",
            "request": "launch",
            "skipFiles": [
                "<node_internals>/**"
            ],
            "type": "pwa-node"
        }
    ]
}